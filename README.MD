How to use this:

run python `init_db.py` to initalize the .db file locally using the schema and data inside the sql folder.
This needs to be manually ran right now whenever changes are made 

then run `python app.py` to get started 
